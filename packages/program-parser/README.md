# @clify/program-parser

Parses a program from a file and outputs a serializeable object representing the program. 

**Disclaimer**
Built to be used with `@clify/clify`. You mileage may vary if used independently.
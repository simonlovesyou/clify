{
  "name": "clify",
  "version": "1.0.0",
  "description": "Main repository for the clify project",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/**"
    ],
    "nohoist": [
      "**/@types/**"
    ]
  },
  "scripts": {
    "test": "lerna run test",
    "build": "yarn build:ts",
    "build:ts": "lerna run build:ts",
    "bootstrap": "lerna bootstrap",
    "clean": "lerna clean && lerna run clean"
  },
  "author": "Simon Johansson <simonljohansson92@gmail.com>",
  "license": "MIT",
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
